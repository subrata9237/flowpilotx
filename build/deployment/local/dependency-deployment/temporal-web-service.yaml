apiVersion: v1
kind: Service
metadata:
  name: temporal-web
  labels:
    app: temporal-web
spec:
  type: NodePort
  selector:
    app: temporal-web
  ports:
    - port: 8088
      targetPort: 8088
      nodePort: 30088