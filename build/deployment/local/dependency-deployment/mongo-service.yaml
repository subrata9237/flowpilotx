apiVersion: v1
kind: Service
metadata:
  name: mongo-wf-storage
spec:
  selector:
    app: mongo-wf-storage
  ports:
    - port: 27017
      targetPort: 27017
      nodePort: 30000  # Expose MongoDB on port 30000 on Minikube's VM
  type: NodePort
